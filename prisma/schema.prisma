datasource db {
  provider = "postgresql"
  url      = env("POSTGREDB_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Teacher {
  id Int  @id @default(autoincrement())
  name String
  school String?
  photoURL String?

  @@unique([name, school])
}