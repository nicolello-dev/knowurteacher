/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  images: {
    domains: ["cdn.knowurteacher.com", "lh3.googleusercontent.com"],
  },
};

module.exports = nextConfig;
